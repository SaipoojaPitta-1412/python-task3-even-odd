# python-task3-even-odd
This Python program checks whether a given number is even or odd using the modulus operator (`%`). Wrapped in a continuous loop with input validation, it handles positive numbers, negative numbers, and zero seamlessly. Users can test multiple inputs consecutively in the command terminal until typing 'exit' to terminate execution safely.
